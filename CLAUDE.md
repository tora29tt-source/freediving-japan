# Claude 作業指示

このリポジトリは **Freediving Japan** プロジェクトです。
作業前に必ず以下の2ファイルを参照してください。

| ファイル | 内容 |
|---|---|
| `STRATEGY.md` | プロジェクト概要・ターゲット・メディア戦略・フェーズロードマップ・収益化設計 |
| `DEV.md` | 技術スタック・DB設計・ファイル構成・実装状況・スケジュール・セキュリティルール |

## Git 運用ルール

**Claude は git コマンドを一切実行しない。**
ファイルの編集のみ行い、commit/push はユーザーが `gcp "メッセージ"` で実行する。

```bash
# ~/.zshrc に定義済み
gcp "commit message"   # add -u → commit → push を一括実行（ロック自動解除付き）
```

*最終更新：2026-06-18*
